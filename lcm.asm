DATA SEGMENT 
N1 DW 019H
N2 DW 00FH
LCM DW 2 DUP(?) 
DATA ENDS 
CODE SEGMENT
ASSUME CS: CODE,DS:DATASTART: 
START:
MOV AX,DATA 
MOV DS,AX 
MOV AX,N1 
MOV BX,N2 
MOV DX,0000H 
AGAIN:
PUSH AX 
PUSH DX 
DIV BX
CMP DX,0000H 
JE EXIT
POP DX 
POP AX 
ADD AX,N1
JNC NINCDX 
INC DX
NINCDX: 
JMP AGAIN 
EXIT:
POP LCM+2 
POP LCM 
MOV AH,4CH 
INT 21H
CODE ENDS 
END START